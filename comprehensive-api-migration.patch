--- a/src/api/config.rs
+++ b/src/api/config.rs
@@ -1,4 +1,4 @@
-use wreq::StatusCode;
+use reqwest::StatusCode;

--- a/src/api/misc.rs  
+++ b/src/api/misc.rs
@@ -1,4 +1,4 @@
-use wreq::StatusCode;
+use reqwest::StatusCode;

--- a/src/services/update.rs
+++ b/src/services/update.rs
@@ -1,4 +1,4 @@
-use wreq::Client;
+use reqwest::Client;

--- a/src/error.rs
+++ b/src/error.rs
@@ -1,7 +1,7 @@
-use wreq::{Response, StatusCode, header::InvalidHeaderValue};
+use reqwest::{Response, StatusCode, header::InvalidHeaderValue};

--- a/src/services/cache.rs
+++ b/src/services/cache.rs
@@ -120,3 +120,3 @@
-    ) -> Option<impl Stream<Item = Result<Bytes, wreq::Error>> + Send + 'static> {
+    ) -> Option<impl Stream<Item = Result<Bytes, reqwest::Error>> + Send + 'static> {
     
@@ -178,3 +178,3 @@
-    stream: impl Stream<Item = Result<Bytes, wreq::Error>> + Send + 'static,
+    stream: impl Stream<Item = Result<Bytes, reqwest::Error>> + Send + 'static,
     
@@ -195,3 +195,3 @@
-fn vec_to_stream(bytes: Vec<Bytes>) -> impl Stream<Item = Result<Bytes, wreq::Error>> {
+fn vec_to_stream(bytes: Vec<Bytes>) -> impl Stream<Item = Result<Bytes, reqwest::Error>> {

--- a/src/gemini_state/mod.rs
+++ b/src/gemini_state/mod.rs
@@ -1,7 +1,7 @@
-use wreq::{Client, ClientBuilder, header::AUTHORIZATION};
+use reqwest::{Client, ClientBuilder, header::AUTHORIZATION};
@@ -118,1 +118,1 @@
-            client.proxy(proxy)
+            client.proxy(reqwest::Proxy::http(&proxy).unwrap())

--- a/src/claude_web_body/request.rs
+++ b/src/claude_web_body/request.rs
@@ -1,7 +1,7 @@
-use wreq::{
+use reqwest::{

--- a/src/claude_code_state/mod.rs
+++ b/src/claude_code_state/mod.rs
@@ -1,8 +1,7 @@
-use wreq::{ClientBuilder, IntoUrl, RequestBuilder};
-use wreq_util::Emulation;
+use reqwest::{ClientBuilder, IntoUrl, RequestBuilder};

--- a/src/claude_web_state/mod.rs  
+++ b/src/claude_web_state/mod.rs
@@ -1,8 +1,7 @@
-use wreq::{
+use reqwest::{
     Client,
     ClientBuilder,
-    Error as ReqError,
+    Error as ReqError, 
     Method,
     RequestBuilder,
     Response,
     Url,
 };
-use wreq_util::Emulation;

--- a/src/claude_web_state/chat.rs
+++ b/src/claude_web_state/chat.rs
@@ -204,1 +204,1 @@
-            .header_append(ACCEPT, "text/event-stream")
+            .header(ACCEPT, "text/event-stream")